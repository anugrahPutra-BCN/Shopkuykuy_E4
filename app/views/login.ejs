<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Login Admin - ShopiKuyKuy</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body { background-color: #ffe6f2; height: 100vh; display: flex; align-items: center; }
        .card { border-radius: 20px; border: none; }
        .btn-pink { background-color: #ff85a2; color: white; border-radius: 10px; }
        .btn-pink:hover { background-color: #ff4d7d; color: white; }
    </style>
</head>
<body>
    <div class="card shadow mx-auto p-4" style="width: 350px;">
        <div class="text-center mb-4">
            <h2>🧁 Admin Login</h2>
        </div>
        <% if(error) { %>
            <div class="alert alert-danger p-2 text-center" style="font-size: 14px;"><%= error %></div>
        <% } %>
        <form action="/login" method="POST">
            <input type="text" name="username" class="form-control mb-2" placeholder="Username" required>
            <input type="password" name="password" class="form-control mb-3" placeholder="Password" required>
            <button type="submit" class="btn btn-pink w-100">Masuk ✨</button>
        </form>
    </div>
</body>
</html>